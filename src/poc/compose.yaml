services:
  web:
    build:
      context: .
      dockerfile: Containerfile
    restart: always
    ports:
      - "5000:5000"
  redis:
    image: "docker.io/library/redis:bookworm"
    restart: always
