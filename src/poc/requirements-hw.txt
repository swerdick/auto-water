# Hardware-interface libraries — installed in the container / on the Pi only.
# Deliberately kept OUT of requirements.txt so `just test` and laptop dev
# installs stay clean: lgpio has no macOS wheel, and Blinka's `import board`
# expects a real Raspberry Pi. The app imports all of these lazily (only in
# sensors/factory.py), so nothing here is needed to run the test suite.
adafruit-blinka
adafruit-circuitpython-hdc302x
adafruit-circuitpython-bh1750
w1thermsensor
gpiozero
lgpio
